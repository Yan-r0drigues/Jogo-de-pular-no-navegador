const filtrarMaioresQue = (n, lista) => {
    let numero = n;
    let listaDeNumeros = lista;

    for (let valor of lista) {
        if (valor > n) {
            let novaLista = [];
            novaLista.push()
        }
    }
}